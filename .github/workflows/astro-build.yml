name: Astro Build

on:
  pull_request:
    branches: [main]
  push:
    branches:
      - main
      - feat/astro-profile-lp

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Build Astro site
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: system-sound-dist
          path: dist
          if-no-files-found: error
